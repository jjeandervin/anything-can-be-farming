# ACBF web

Angular development runs in Docker through the Aspire AppHost. See the [root README](../../README.md) for setup, Keycloak configuration, and verification.

Public runtime configuration is generated from AppHost environment variables; `config.example.json` supplies defaults. Never put secrets in frontend configuration.
